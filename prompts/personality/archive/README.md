# Personality Archive

This folder stores superseded or reference-only personality prompt specs.

Archive logic:

- Keep legacy prompt versions here when they no longer represent the maintained default workflow but still matter for history, comparison, or older runner references.
- Preserve legacy filenames when that helps trace older documentation or scripts.
- Maintained shell wrappers should point at `../` prompt files, not this archive, unless a legacy run is explicitly intended.
