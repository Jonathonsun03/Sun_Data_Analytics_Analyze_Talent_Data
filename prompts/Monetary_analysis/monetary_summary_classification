Use this summarization prompt file as the source of truth:
<PROMPT_FILE_PATH>

Root directory:
<TALENT_ROOT_PATH>

Process all talent folders under root, excluding:
<EXCLUDED_FOLDER_NAMES_OR_PATTERNS>

For each talent folder:
1) Read CSV files in `text_playback/`.
2) Create `stream_summary_codex/` if missing.
3) For each CSV that does not already have a matching summary, generate one summary strictly following the prompt file.
4) Output filename format: <original_csv_basename>_summary.md
5) Save outputs in that talent’s `stream_summary_codex/`.

Execution controls:
- Refresh yourself with the prompt file every <N> streams.
- Idempotent run: skip existing `_summary.md` files (do not overwrite unless I explicitly ask).
- Start with 1 pilot file and wait for my approval, then continue with the full batch.

Final validation/report:
- Per talent, report: csv_count, summary_count, pending_count.
- Verify each summary contains required section headings from the prompt.
- Report any failures/missing files.
