# Shared Qualities Archive

This folder stores superseded or reference-only shared-baseline prompt specs.

Archive logic:

- Keep earlier shared-baseline prompt drafts here when they are no longer the maintained default.
- Leave the active prompt in the parent folder so the current shell target stays easy to identify.
