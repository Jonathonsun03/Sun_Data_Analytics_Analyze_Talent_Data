# Summary Archive

This folder stores superseded or reference-only summary prompt specs.

Archive logic:

- Keep legacy summary prompt versions here when they are useful for comparison or historical runner context.
- Do not mix archived variants into the top level of `prompts/summaries/`, which should stay focused on maintained defaults.
