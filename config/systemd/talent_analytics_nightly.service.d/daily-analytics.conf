[Service]
# The collector succeeds before this post-step is run. The wrapper loads this
# repository's .env to resolve the same unified lakehouse.
Environment=RENV_CONFIG_AUTOLOADER_ENABLED=FALSE
ExecStartPost=/home/jonathon/sun_data_analytics_projects/Sun_Data_Analytics_Analyze_Talent_Data/bin/linux/analytics/run_daily_analytics.sh --execute
