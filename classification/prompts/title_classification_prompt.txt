Classify each YouTube title record into the JSON schema provided below.

Rules:
1. Return JSON only. Do not include markdown, comments, or extra keys.
2. Return one item for every input record and preserve the same `video_id`.
3. Confidence must be a number between 0 and 1.
4. Use only enum values listed in the schema.
5. Keep `tags` concise and relevant (up to maxItems from schema).

Output schema:
{{schema_json}}

Input records:
{{records_json}}
